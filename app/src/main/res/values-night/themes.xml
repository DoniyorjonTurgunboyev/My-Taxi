<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyTaxiTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/selectedDark</item>
        <item name="colorPrimaryVariant">@color/unSelectedDark</item>
        <item name="colorOnPrimary">@color/clickColorDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/startColorDark</item>
        <item name="colorSecondaryVariant">@color/endColorDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>